spring:
    datasource:
        driver-class-name: com.mysql.cj.jdbc.Driver
        username: root
        url: jdbc:mysql://localhost:3306/shop
        password: root
    application:
        name: shop
    jpa:
        properties:
            hibernate:
                show_sql: 'true'
        hibernate:
            ddl-auto: update
